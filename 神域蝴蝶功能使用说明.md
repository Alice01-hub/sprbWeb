# 🦋 神域蝴蝶功能使用说明

## 📚 功能概述

神域蝴蝶系统是一个互动性的记忆展示功能。每次访问神域页面或切换场景时，会随机显示3-5只飞舞的蝴蝶，点击蝴蝶可以查看其中包含的记忆内容。

## 🎯 主要特性

### 1. 动态展示
- 每次切换场景随机显示3-5只蝴蝶
- 蝴蝶位置随机分布，自动避免重叠
- 蝴蝶在图片范围内飞舞，不超出边界

### 2. 飞舞动画
- 使用两张图片交替播放（每400ms切换一次）
- 蝴蝶带有轻微的上下浮动效果（3秒循环）
- 悬停时蝴蝶会轻微放大（1.1倍）

### 3. 权重尺寸系统
蝴蝶的大小由`weight`字段决定（1-5）：

| Weight | 尺寸 | 适用场景 |
|--------|------|----------|
| 1 | 20px | 小型装饰蝴蝶 |
| 2 | 36px | 次要内容 |
| 3 | 52px | 普通内容（默认） |
| 4 | 68px | 重要内容 |
| 5 | 84px | 特别重要的内容 |

### 4. 多媒体内容支持
每只蝴蝶可以包含：
- **文字内容**: 记忆的详细描述
- **图片**: 相关的照片或插图
- **音频**: 音乐或语音留言
- **链接**: 相关的外部网页

## 🗄️ 数据库设置

### 步骤1: 创建数据表

1. 登录 [Supabase Dashboard](https://supabase.com/dashboard)
2. 选择你的项目
3. 进入 **SQL Editor**
4. 复制 `backend/create_memories_table.sql` 的内容
5. 点击 **Run** 执行

### 步骤2: 验证表创建

进入 **Table Editor**，应该能看到 `memories` 表，包含以下字段：
- `id` - 自增主键
- `created_at` - 创建时间
- `user_name` - 作者名称
- `title` - 标题
- `content` - 内容
- `image_url` - 图片链接
- `audio_url` - 音频链接
- `web_url` - 网页链接
- `weight` - 权重（1-5）
- `is_published` - 是否发布

## 📝 添加蝴蝶内容

### 方法1: 在Supabase Dashboard中添加

1. 进入 **Table Editor**
2. 选择 `memories` 表
3. 点击 **Insert row**
4. 填写字段：
   - `user_name`: 作者名称（必填）
   - `title`: 标题（必填）
   - `content`: 内容（可选）
   - `image_url`: 图片URL（可选）
   - `audio_url`: 音频URL（可选）
   - `web_url`: 网页链接（可选）
   - `weight`: 1-5（默认3）
   - `is_published`: 勾选以发布
5. 点击 **Save**

### 方法2: 使用SQL插入

```sql
INSERT INTO memories (
  user_name, 
  title, 
  content, 
  image_url,
  audio_url,
  web_url,
  weight, 
  is_published
) VALUES (
  '作者名称',
  '记忆标题',
  '这是一段美好的回忆...',
  'https://example.com/image.jpg',
  'https://example.com/audio.mp3',
  'https://example.com/link',
  3,
  true
);
```

### 方法3: 批量导入

在 **SQL Editor** 中执行批量插入：

```sql
INSERT INTO memories (user_name, title, content, weight, is_published) VALUES
('旅行者A', '夏日回忆', '在鸟白岛的夏天...', 5, true),
('旅行者B', '海边约定', '我们在海边许下了约定...', 4, true),
('旅行者C', '星空物语', '满天繁星的夜晚...', 3, true);
```

## 🎨 内容建议

### 标题（title）
- 简短有力，5-10个字
- 能够概括记忆的主题
- 例如："七影蝶的传说"、"夏日海滩"、"星空下的誓言"

### 内容（content）
- 详细描述记忆或故事
- 50-500字为宜
- 可以使用换行符增强可读性
- 情感真挚，引起共鸣

### 图片（image_url）
- 建议尺寸：800-1200px宽度
- 支持格式：JPG, PNG, WebP
- 使用OSS或图床服务
- 确保图片链接稳定可访问

### 音频（audio_url）
- 支持格式：MP3, WAV, OGG
- 建议时长：30秒-3分钟
- 音质清晰，音量适中
- 内容可以是音乐、语音、自然声音等

### 链接（web_url）
- 相关的外部网页
- 可以是博客文章、视频、图集等
- 确保链接有效且内容相关

### 权重（weight）选择指南

**Weight 5 (最大)**
- 特别重要的核心内容
- 官方公告或重要消息
- 精选的优质记忆

**Weight 4**
- 重要但非核心的内容
- 高质量的用户投稿
- 有意义的纪念内容

**Weight 3 (默认)**
- 普通的记忆分享
- 日常的游记内容
- 常规的互动内容

**Weight 2**
- 轻量级的小故事
- 简短的心情记录
- 装饰性的短内容

**Weight 1 (最小)**
- 纯装饰性蝴蝶
- 极简短的语句
- 留白和点缀效果

## 🔧 管理蝴蝶内容

### 隐藏蝴蝶
将 `is_published` 设置为 `false`：

```sql
UPDATE memories SET is_published = false WHERE id = 1;
```

### 显示蝴蝶
将 `is_published` 设置为 `true`：

```sql
UPDATE memories SET is_published = true WHERE id = 1;
```

### 删除蝴蝶
```sql
DELETE FROM memories WHERE id = 1;
```

### 修改权重
```sql
UPDATE memories SET weight = 5 WHERE id = 1;
```

### 批量管理
```sql
-- 隐藏所有weight为1的蝴蝶
UPDATE memories SET is_published = false WHERE weight = 1;

-- 显示所有weight大于等于3的蝴蝶
UPDATE memories SET is_published = true WHERE weight >= 3;

-- 删除所有未发布的蝴蝶
DELETE FROM memories WHERE is_published = false;
```

## 📊 查询和统计

### 查看已发布的蝴蝶
```sql
SELECT * FROM memories 
WHERE is_published = true 
ORDER BY weight DESC, created_at DESC;
```

### 统计蝴蝶数量
```sql
SELECT 
  COUNT(*) as total,
  SUM(CASE WHEN is_published = true THEN 1 ELSE 0 END) as published,
  SUM(CASE WHEN is_published = false THEN 1 ELSE 0 END) as unpublished
FROM memories;
```

### 按权重分组统计
```sql
SELECT 
  weight,
  COUNT(*) as count,
  SUM(CASE WHEN is_published = true THEN 1 ELSE 0 END) as published_count
FROM memories
GROUP BY weight
ORDER BY weight DESC;
```

### 查看最近添加的蝴蝶
```sql
SELECT * FROM memories 
ORDER BY created_at DESC 
LIMIT 10;
```

## 🐛 常见问题

### Q1: 为什么没有蝴蝶显示？
**A:** 检查以下几点：
1. 数据库中是否有 `is_published = true` 的记录
2. 浏览器控制台是否有错误信息
3. Supabase连接是否正常
4. 前端是否正确加载了 `memoryService.ts`

### Q2: 蝴蝶重叠怎么办？
**A:** 系统已经实现了防重叠算法，但如果蝴蝶数量太多可能会出现重叠。可以：
1. 减少发布的蝴蝶数量
2. 调整 `memoryService.ts` 中的 `minDistance` 参数
3. 增大场景图片尺寸

### Q3: 如何调整蝴蝶飞舞速度？
**A:** 在 `MemoryButterfly.tsx` 中修改：
- 翅膀扇动速度：修改 `setInterval` 的时间参数（默认400ms）
- 浮动动画速度：修改 `transition.y.duration`（默认3秒）

### Q4: 如何更换蝴蝶图片？
**A:** 在 `MemoryButterfly.tsx` 中修改 `BUTTERFLY_IMAGES` 数组的URL

### Q5: 如何调整蝴蝶尺寸？
**A:** 有两种方法：
1. 修改数据库中的 `weight` 值（1-5）
2. 调整 `calculateButterflySize` 函数中的基础尺寸和缩放系数

## 🎯 最佳实践

### 内容质量
- 确保每个蝴蝶都有意义的内容
- 避免纯广告或无关内容
- 保持内容的积极性和正能量

### 数量控制
- 建议维持10-30只已发布蝴蝶的池子
- 定期更新内容，保持新鲜感
- 根据季节或活动调整蝴蝶内容

### 权重分配
- 不要将所有蝴蝶设为weight 5
- 保持权重分布的平衡
- 建议比例：weight 5:4:3:2:1 = 1:2:4:2:1

### 多媒体使用
- 不是所有蝴蝶都需要图片和音频
- 多媒体内容要与主题相关
- 确保多媒体资源的加载速度

## 🚀 高级功能（未来可扩展）

以下功能可以在未来版本中添加：

- [ ] 用户投稿系统
- [ ] 点赞和收藏功能
- [ ] 分类和标签系统
- [ ] 搜索功能
- [ ] 管理后台
- [ ] 数据统计面板
- [ ] 蝴蝶分享功能
- [ ] 评论系统

## 📞 技术支持

如遇到问题，可以：
1. 查看浏览器控制台的错误信息
2. 检查Supabase Dashboard的日志
3. 参考项目README.md的相关章节
4. 在项目仓库提交Issue

---

**祝你在神域中留下美好的记忆！** 🦋✨

